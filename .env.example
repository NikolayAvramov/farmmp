# Supabase — копирай в .env.local (не комитирай ключове)
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=""

# Един скрипт за всички таблици + RLS (култури, задачи, склад, клиенти, поръчки, разходи, todos):
#   supabase/schema.sql
#
# CRUD през Next.js Route Handlers: app/api/* (Supabase само на сървъра с cookies).
